apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gptchart.fullname" . }}-test-connection"
  labels:
    {{- include "gptchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gptchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
