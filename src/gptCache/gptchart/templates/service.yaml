apiVersion: v1
kind: Service
metadata:
  name: {{ include "gptchart.fullname" . }}
  labels:
    {{- include "gptchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gptchart.selectorLabels" . | nindent 4 }}
